default:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account
      project: bitcoin-project-444715
      dataset: raw_dataset
      threads: 4
      keyfile: secrets.GCP_CREDENTIALS
      priority: interactive
