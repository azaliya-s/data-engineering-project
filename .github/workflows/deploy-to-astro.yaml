name: Astronomer CI - Deploy code
on:
  workflow_dispatch:
    inputs:
      deploymentId:
        description: "Deployment ID"
        required: true
        type: string
      apiToken:
        description: "Astro API Token"
        required: true
        type: string
  push:
    branches:
      - main
env:
  ASTRO_API_TOKEN: ${{ inputs.apiToken || secrets.ASTRO_API_TOKEN }}
  DEPLOYMENT_ID: ${{ inputs.deploymentId || vars.DEPLOYMENT_ID }}


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Authenticate with Google Cloud
      uses: google-github-actions/auth@v1
      with:
        credentials_json: ${{ secrets.GCP_CREDENTIALS }}

    - name: Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@v1
    - name: Debug Info
      run: |
       echo "DEPLOYMENT_ID length: ${#DEPLOYMENT_ID}"
       echo "WORKSPACE_ID length: ${#WORKSPACE_ID}"
       echo "API TOKEN length: ${#ASTRO_API_TOKEN}"
       echo "DEPLOYMENT_ID starts with: ${DEPLOYMENT_ID:0:5}..."
       echo "WORKSPACE_ID starts with: ${WORKSPACE_ID:0:5}..."

    - name: Deploy to Astro
      uses: astronomer/deploy-action@v0.4
      env:
        ASTRO_WORKSPACE_ID: ${{ env.WORKSPACE_ID }}
      with:
        deployment-id: ${{ env.DEPLOYMENT_ID }}
        token: ${{ env.ASTRO_API_TOKEN }}
        workspace-id: ${{ env.WORKSPACE_ID }} 
